name: network
description: "Networking layer: Dio factory, interceptors, error mapping, retry."
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0
  flutter: 3.47.0

dependencies:
  core:
    path: ../core
  dio: 5.11.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
