name: auth
description: "Auth feature: login, session persistence, token refresh."
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0
  flutter: 3.47.0

dependencies:
  core:
    path: ../core
  equatable: 3.0.0
  flutter:
    sdk: flutter
  flutter_secure_storage: 11.2.0
  json_annotation: 4.12.0
  network:
    path: ../network

dev_dependencies:
  build_runner: 2.16.1
  flutter_test:
    sdk: flutter
  json_serializable: 6.14.1
